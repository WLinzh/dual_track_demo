{
  "name": "api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "source venv/bin/activate && python -m uvicorn main:app --reload --host 0.0.0.0 --port 3001",
    "start": "source venv/bin/activate && python -m uvicorn main:app --host 0.0.0.0 --port 3001"
  }
}
